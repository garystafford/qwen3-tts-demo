# XTTS v2 Dependencies (Windows Local)
#
# Setup (requires Python 3.11, CUDA 12.1):
#   1. Install PyTorch with CUDA FIRST (do NOT skip this):
#      py -3.11 -m pip install --force-reinstall torch==2.4.0 torchaudio==2.4.0 --index-url https://download.pytorch.org/whl/cu121
#   2. Then install these dependencies:
#      py -3.11 -m pip install -r requirements-xtts.txt
#
# WARNING: Do NOT add torch or torchaudio here - it will replace the CUDA version with CPU-only.

# Coqui TTS (includes XTTS v2)
TTS>=0.22.0

# Gradio for web UI
gradio

# Audio processing
soundfile
numpy

# Pin transformers for Coqui TTS compatibility
transformers==4.40.0
